# ASGI framework
fastapi==0.100.0

# Uvicorn for running FastAPI
uvicorn[standard]==0.23.1

# PDF processing (in-memory, full MuPDF API)
PyMuPDF==1.23.8

# Handling multipart/form-data in FastAPI
python-multipart==0.0.6

# (Optional) ZIP utilities if you need explicit zipfile support
# zipfile is in stdlib—no install needed

# DEV / TEST (optional)
# pytest==7.4.0
# pytest-asyncio==0.21.0
